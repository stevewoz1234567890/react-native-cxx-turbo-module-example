# Purplemana Frontend

This is the first pass at a new React-based frontend.  Full TypeScript, and Webpack support.  Leveraging Material-UI (specifically the DataGrid component for rendering the buylist).

### Local Development

1. Run `npm ci`
2. Run `npm start`
3. Open `http://localhost:8080/` (should open automatically)